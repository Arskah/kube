apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: homepage-staging
  namespace: argocd
spec:
  destination:
    namespace: homepage
    server: {{ .Values.spec.destination.server }}
  project: default
  source:
    path: homepage-staging
    repoURL: {{ .Values.spec.source.repoURL }}
    targetRevision: {{ .Values.spec.source.targetRevision }}
